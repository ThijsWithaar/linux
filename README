Script to build a linux kernel.
- Kernel sources from upstream
- Using debian kernel configuration as base

The following modifications are made to the kernel configuration:
- AMDGPU: enable shared memory
- Use ZSTD everywhere
- Compile with clang and thin-LTO, march=native
- Include some options instead of kernel modules (drm, amdgpu, virtualbox, bcachefs)
